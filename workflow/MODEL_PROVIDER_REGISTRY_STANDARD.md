# Model Provider Registry Standard

Every provider should be added through an adapter and registry pattern.

Keep discovery, enrichment, classification, extraction, ranking, geocoding, storage, notification, and visualization separate.
