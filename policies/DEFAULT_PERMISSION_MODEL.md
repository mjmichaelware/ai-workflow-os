# Permission Model

Default auto actions: inspect repo, read files, create plan.

Default approval actions: write files, read secrets, execute shell, call live APIs, git commit, git push, cloud writes, deploy.
