# Live Agent Roadmap

Stage 1 created the generic CLI and dashboard.
Stage 2 adds provider registry, Secret Manager references, permission gates, and dry-run agent planning.
Stage 3 adds concrete tool adapters, run logs, approval manifests, and safer execution loops.

The agent must always be dry-run first. Live side effects require explicit approval.
