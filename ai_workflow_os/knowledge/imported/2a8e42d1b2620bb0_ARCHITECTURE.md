# Architecture

AI Workflow OS is a generic workflow control plane with Python CLI, local dashboard, catalog registry, project installer, handoff packet exporter, standards library, and reusable templates.
