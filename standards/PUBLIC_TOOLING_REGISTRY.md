# Public Tooling Registry

Generic extendable tool categories: git, gh, gcloud, python3, uv, ruff, pytest, node, npm, jq, rg, tar, gzip, model APIs, embedding APIs, search APIs, maps APIs, storage APIs, notification APIs, and visualization APIs.
