# AI Model Safety Standard

LLMs may summarize, classify, extract, rank, draft, and enrich. They must not invent proof, print secrets, call costly endpoints without authorization, deploy without authorization, or be treated as ground truth without evidence.
