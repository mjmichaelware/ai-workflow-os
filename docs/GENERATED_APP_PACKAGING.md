# Generated App Packaging

AI Workflow OS generated apps now receive a standard shell.

The shell includes a local PWA, icon, service worker, builder, Android wrapper scaffold, proof packet, and compressed tar packet.

The Android wrapper folder is a scaffold. A signed APK build is a later explicit stage.
